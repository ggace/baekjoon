#include <stdio.h>

int main() {
    int n;

    scanf("%d", &n);

    for(int i = 0; i < n; i++){
        int a, b;

        for(int i = 0; i < 4; i++){
            scanf("%d %d", &a, &b);

        }

    }
    return 0;
}